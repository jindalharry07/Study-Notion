<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Content | StudyNotion Instructor</title>
  <link rel="stylesheet" href="/css/edit-content.css">
</head>
<body>
  <div class="edit-container">
    <h2>Edit Uploaded Notes</h2>

    <form method="POST" action="/instructor/edit/<%= content._id %>">
      <label for="title">Title:</label>
      <input type="text" name="title" id="title" value="<%= content.title %>" placeholder="Enter title" required />

      <label for="description">Description:</label>
      <textarea name="description" id="description" rows="5" placeholder="Write a description..." required><%= content.description %></textarea>

      <div class="form-actions">
        <button type="submit">Update</button>
        <a href="/instructor/dashboard" class="cancel-btn">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
