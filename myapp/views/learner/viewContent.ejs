<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Content</title>
  <link rel="stylesheet" href="/css/learner-content.css">
</head>
<body>
  <header class="navbar">
    <div class="logo">👨‍🎓 StudyNotion</div>
    <a href="/learner" class="back-link">Back to Content List</a>
  </header>

  <main class="content-container">
    <h1><%= content.title %></h1>
    <p><%= content.description %></p>

    <% if(content.type === 'pdf') { %>
      <embed src="/uploads/<%= content.fileName %>" type="application/pdf" width="100%" height="600px">
    <% } else if(content.type === 'quiz') { %>
      <p>Quiz link: <a href="<%= content.quizLink %>" class="quiz-link">Start Quiz</a></p>
    <% } else { %>
      <p>Unsupported content type.</p>
    <% } %>
  </main>
</body>
</html>
